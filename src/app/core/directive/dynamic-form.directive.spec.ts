import { DynamicFormDirective } from './dynamic-form.directive';

describe('DynamicFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicFormDirective();
    expect(directive).toBeTruthy();
  });
});
