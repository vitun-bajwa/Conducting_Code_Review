export interface currentUser {
    id?: string,
    firstName?: string,
    lastName?: string,
    email?: string,
    password?: string,
    userRole?: string,
    AddUser?: Array<object>,
    status?: string,
    createdBy?: string
}